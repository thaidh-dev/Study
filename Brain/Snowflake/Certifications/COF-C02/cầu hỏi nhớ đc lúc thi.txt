1: This table function can be used to query information about the staged data files included in the metadata for a specified external table.
EXTERNAL_TABLE_FILE_REGISTRATION_HISTORY
EXTERNAL_TABLE_FILES (correct answer)
EXTERNAL_FUNCTIONS_HISTORY
DYNAMIC_TABLES

2
What can be shared when the SECURE_OBJECTS_ONLY property is set to FALSE?
A. A view
B. A stored procedure
C. Storage integration access
D. A User-Defined Function (UDF)


3
max file size when single = true (copy into location) => 5GB

4
nhiều copy into table (đọc 1 file duy nhất) run cùng lúc 
+ data sẽ bị trùng
+ báo lỗi
+ chỉ 1 file đc load

5
table nào ko thể dùng update command (2 đáp án)
+ temporary
+ hybrid
+ dynamic
+ transient
+ external table

6
In Snowflake's Snowpark, DataFrame operations are processed when 
+ collect
+ an action is triggered
+ quên mẹ nó rồi

7 earch Optimization Service benefit when
+ query with COLLATE
+ query with cast funtion 
+ query with (quên mẹ nó rồi)
+ query with "and" or "or"

8 ORGADMIN can create
+ account
+ reader account
+ replication
+ User
+ role

9 user type "service" thì login như nào
https://docs.snowflake.com/en/user-guide/admin-user-management#types-of-users
+ password
+ SAML SSO
+ MFA
+ Key-pair authentication (correct answer)


10 vs array mà trong đó có nhiều nest nhiều array, trong các array con lại nest nhiều array khác nữa
+ dùng flatten có thể tự đọc đc tất cả từ thằng cha đến con
+ flatten chỉ có thể đọc mỗi thằng đầu tiên